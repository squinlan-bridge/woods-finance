import React from "react";
import ReactDOM from "react-dom/client";
import App from "../woods_finance_app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
