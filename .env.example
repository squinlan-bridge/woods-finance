# Copy to .env and fill in. NEVER commit .env.
# Supabase — the WOODS project (zeewewahazzuzofhvwuu), NOT Bridge's.
export SUPABASE_URL=https://zeewewahazzuzofhvwuu.supabase.co
# service_role key: Supabase dashboard → Project Settings → API keys
export SUPABASE_SERVICE_KEY=
# Intuit developer app (same app as Bridge — copy from ~/bridge-finance/.env)
export QBO_CLIENT_ID=
export QBO_CLIENT_SECRET=
# Optional: earliest transaction date to sync (default 2023-06-01)
# export WOODS_START_DATE=2023-06-01
